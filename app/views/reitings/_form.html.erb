<%= form_with(model: reiting) do |form| %>
  <% if reiting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reiting.errors.count, "error") %> prohibited this reiting from being saved:</h2>

      <ul>
        <% reiting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reiting, style: "display: block" %>
    <%= form.number_field :reiting %>
  </div>

  <div>
    <%= form.label :id_tovar, style: "display: block" %>
    <%= form.number_field :id_tovar %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
