<%= form_with(model: tovar) do |form| %>
  <% if tovar.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tovar.errors.count, "error") %> prohibited this tovar from being saved:</h2>

      <ul>
        <% tovar.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :org_id, style: "display: block" %>
    <%= form.number_field :org_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
